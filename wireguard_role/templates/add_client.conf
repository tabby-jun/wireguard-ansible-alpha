[Interface]
Address = {{ new_client_ip }}/32
DNS = {{ vpn_network }}.1
PrivateKey = {{ new_private_key_file.stdout }}

[Peer]
PublicKey = {{ server_public_key_file.stdout }}
AllowedIPs = 0.0.0.0/0
Endpoint = {{ inventory_hostname }}:{{ vpn_port }}
PersistentKeepalive = 21